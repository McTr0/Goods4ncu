# gugugaga 角色模型放置目录

将以下文件放入本目录即可启用该角色（无需改代码）：

    Gugugaga.model3.json   （及其中引用的 .moc3 / 贴图 / physics / expressions）

选择方式：

    flutter run -d chrome --dart-define=COMPANION_CHARACTER=gugugaga

未放置模型时选择 gugugaga 会自动回退到 doro。
